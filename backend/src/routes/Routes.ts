// Imports.
import { Application } from "express";
import SoftwareLicenser from "../controller/SoftwareLicenser";

/**
 * Export the representative.
 */
export default {
  register: (application: Application, licenser: SoftwareLicenser) => {
    
  }
}; 